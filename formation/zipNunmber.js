
/*

3. Zip Two Arrays

Given two arrays of integers of equal length, zip them together by alternating elements from each array. Return a new array where elements from both input arrays are interleaved, starting with the first element of the first array, followed by the first element of the second array, and so on.
Example(s)
**Input:** array1 = [1, 2], array2 = [3, 4]
**Output:** [1, 3, 2, 4]

**Input:** array1 = [5, 10, 15], array2 = [0, 1, 2]
**Output:** [5, 0, 10, 1, 15, 2]

newArry = []
for i: len(a){
  newArray.push(a[i]).push(b[i])
}

array1 = [1, 2, 3, 4, 5], array2 = [3, 4, 5]
result = [1, 3, 2, 4, 3, 5, 4, 5]

minArray = a
maxArray =b
if (a.length >= b.length){
max= a
min =b
}

newArry = []
for i: max.len -1{
if (i < min.length){}
  newArray.push([i]).push(b[i])
}
split = a.split(m, n)
newArray.concat(split)


*/